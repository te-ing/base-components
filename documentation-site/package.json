{
  "name": "documentation-site",
  "version": "0.1.0",
  "private": true,
  "description": "Documentation site for base components",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "base-components": "workspace:*",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
